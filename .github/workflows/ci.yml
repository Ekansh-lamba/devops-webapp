name: CI Pipeline

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: Run a one-line script
      run: echo "Starting the build process..."

    - name: Simulate Testing
      run: echo "Running tests... All tests passed!"git 